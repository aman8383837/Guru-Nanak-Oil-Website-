export const ErrorFallback = ({ error, resetErrorBoundary }: any) => {
  return (
    <div className="min-h-screen flex items-center justify-center p-4 bg-slate-50">
      <div className="text-center max-w-md bg-white p-8 rounded-2xl border border-gray-150 shadow-md">
        <h2 className="text-2xl font-black text-red-600 mb-4 uppercase">Something Went Wrong!</h2>
        <p className="text-gray-600 mb-2 font-medium">{error?.message || 'An unexpected error occurred.'}</p>
        <p className="text-sm text-gray-400 mb-6 font-semibold">Please refresh the page or try again later.</p>
        <button
          onClick={resetErrorBoundary}
          className="px-6 py-3 bg-primary hover:bg-amber-500 hover:text-primary text-white font-black rounded-xl transition-all shadow-md cursor-pointer uppercase text-xs tracking-wider"
        >
          Try Again
        </button>
      </div>
    </div>
  );
};
